<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Layout.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">
            <Label Text="Hello" BackgroundColor="Silver" VerticalOptions="Center"
                 HorizontalOptions="Center" WidthRequest="100" HeightRequest="300" FontSize="40"/>
            <StackLayout>
                <Label Text="Start" HorizontalOptions="Start" BackgroundColor="Silver" FontSize="40" />
                <Label Text="Center" HorizontalOptions="Center" BackgroundColor="Silver"  FontSize="40" />
                <Label Text="End" HorizontalOptions="End" BackgroundColor="Silver"  FontSize="40"/>
                <Label Text="Fill" HorizontalOptions="Fill" BackgroundColor="Silver"  FontSize="40"/>
            </StackLayout>
            <StackLayout Spacing="30">
                <BoxView Color="Silver" />
                <BoxView Color="Blue" />
                <BoxView Color="Gray" />
            </StackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
